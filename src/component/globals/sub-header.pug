block vars

.wrap
  img(
    src=`/images/main-${INDEX}.png`
    srcset=`/images/main-${INDEX}.png 1x, /images/main-${INDEX}@2x.png 2x`
    width="900"
    height="770"
  )
  img.sp(
    src=`/images/main-${INDEX}-sp.png`
    srcset=`/images/main-${INDEX}-sp.png 1x, /images/main-${INDEX}-sp@2x.png 2x`
    width="482"
    height="auto"
  )
//-   .hero-title
//-     span.catch あたらしい<br />ハロウィン
//-     spam.num.font-style-2 #{INDEX}
//-     .copy
//-       each item in COPY
//-         span.inline-block #{item}
//-     img(width="80px" src="/images/split-mask.svg")
//-   .hero-content
//-     img(src=`/images/content-${INDEX}.jpg`)
//-     .body
//-       if HEAD
//-         .headline
//-           each li in HEAD
//-             span.inline-block !{li}
//-       each item, i  in BODY
//-         if INDEX == "03" && (i == 1 || i == 2)
//-           span.inline-block.under !{item}
//-         else
//-           span.inline-block !{item}
//-       if AT
//-         .attention
//-           each li in AT
//-             .li !{li}
//- .hero-sub
//-   if HEAD
//-     .headline
//-       each li in HEAD
//-         .li !{li}
//-   each item in BODY
//-     span.inline-block !{item}
//-   if AT
//-     .attention
//-       each li in AT
//-         .li !{li}